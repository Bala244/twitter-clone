<!DOCTYPE html>
<html>
  <head>
    <title>TwitterClone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="navbar-test">
      <div>
        <span>Rails Twitter Clone</span>
        <% if user_signed_in?  %>
          Logged In as <strong><%= current_user.email%></strong>
      </div>
      <div>
          <%= link_to 'New Tweet', new_tweet_path, :class => 'btn' %> |
          <%= link_to 'My profile', user_profile_path(current_user.id), :class => 'navbar-link' %> |
          <%= link_to 'Edit Profile', edit_user_registration_path, :class => 'navbar-link' %> |
          <%= link_to 'Logout', destroy_user_session_path, method: :delete, :class => 'navbar-link' %>
      </div>
      <% else %>
      </div>
      <div>
        <%= link_to 'Sign Up', new_user_registration_path, :class => 'navbar-link' %> |
        <%= link_to 'Login', new_user_session_path, method: :delete, :class => 'navbar-link' %>
      </div>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
